name: "Ansible Playbook"
description: "Run Ansible Playbooks"
inputs:
  project:
    required: true
    description: "Environment services"
  service:
    required: true
    description: "Service name"
runs:
  using: "composite"
  steps:
    - name: Testing
      shell: bash
      run: echo $PROJECT $SERVICE
      env:
        PROJECT: ${{ inputs.project }}
        SERVICE: ${{ inputs.service }}

    - name: Testing2
      shell: bash
      run: echo $PROJECT $SERVICE
      env:
        PROJECT: ${{ inputs.project }}
        SERVICE: ${{ inputs.service }}
