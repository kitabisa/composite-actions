name: "Get GHCR Credential"
description: "Get GHCR Credential"

runs:
  using: "composite"
  steps:
    - name: Log in to Helm Registry (GHCR) 
      shell: bash
      run: |
          helm registry login ghcr.io --username $GITHUB_ACTOR --password "${{ secrets.GITHUB_TOKEN }}"